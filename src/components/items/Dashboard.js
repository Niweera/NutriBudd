import React, { Component } from "react";

class Dashboard extends Component {
  render() {
    return (
      <div className="container">
        {/* Add the dashboard content here which will be shown after the login */}
        <h1>This is the dashboard page.the Content will be added here.</h1>
      </div>
    );
  }
}

export default Dashboard;
